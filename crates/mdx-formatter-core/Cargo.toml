[package]
name = "mdx-formatter-core"
version = "0.1.0"
edition = "2021"

[dependencies]
markdown = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3"

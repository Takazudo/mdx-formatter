[package]
name = "mdx-formatter-napi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
mdx-formatter-core = { path = "../mdx-formatter-core" }
napi = { version = "2", features = ["default"] }
napi-derive = "2"
serde_json = "1"

[build-dependencies]
napi-build = "2"
